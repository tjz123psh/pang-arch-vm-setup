# Optional AUR packages for daily desktop app usage.
# Installed only with ./install.sh --with-apps.
# These are binary/repackaged apps, so review PKGBUILDs before installing.

google-chrome
linuxqq-appimage
obsidian-bin
wechat-universal-bwrap
